apiVersion: 1

datasources:
  # Prometheus 数据源 (指标)
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  # Loki 数据源 (日志)
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: true